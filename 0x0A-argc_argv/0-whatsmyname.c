#include <stdio.h>
/**
 * main - is a function to get argc and argv
 * @argc: is the number of word include the program name
 * @argv: is the
 * Return: return 0
 */
int main(int argc, char const *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
