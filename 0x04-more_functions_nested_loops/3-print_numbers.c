#include "main.h"
/**
 * print_numbers - function to check uppercase
 *
 *Return: 0 is success
 *
 */
void print_numbers(void)
{
	int i;

	for (i == 0 ; i <= 9 ; i++)
		_putchar(i + 48);
	_putchar('\n');
}
