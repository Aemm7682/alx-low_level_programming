#include "main.h"
/**
 * _isupper - function to check uppercase
 *
 * @c: is input to check
 *
 *Return: 1 if uppercase and 0 if not
 *
 */
void print_numbers(void)
{
	int i;

	for (i == 0 ; i <= 9 ; i++)
		_putchar(i + 48);
	_putchar('\n');
	return (0);
}
