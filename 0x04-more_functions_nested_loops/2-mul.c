#include "main.h"
/**
 * mul - function to check uppercase
 *
 * @a: is input to check
 *
 * @b: is input to check
 * Return: mul
 *
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
