#include "main.h"
/**
 * _isdigit - function to check digit
 *
 * @c: is input to check
 *
 *Return: 1 if digit and 0 if not
 *
 */
char *_strpbrk(char *s, char *accept)
{
                return (0);
}
